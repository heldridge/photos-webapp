{% extends 'base.html.j2' %}

{% load static %}

{% block content %}

<form method="post" class='flex justify-center mt-10'>
    {% csrf_token %}
    <div class="">
        {% for field in form %}
        <div>
            <div class="flex justify-between items-center">
                <span class="text-lg opacity-high-emphasis mr-5">{{ field.label_tag }}</span>
                {{ field }}
            </div>
            
            {% for error in field.errors %}
            <p class="text-error">{{ error }}</p>
            {% endfor %}
        </div>
        {% endfor %}
        <div class="flex justify-center">
            <button type="submit" class="text-lg rounded mt-5 py-2 px-4 bg-primary hover:bg-primary-hover text-black">Change Password</button>
        </div>
    </div>
</form>

<script type="text/javascript" src="{% static 'js/basic.js' %}"></script>

{% endblock %}